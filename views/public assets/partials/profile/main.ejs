
<%
  var basePath = (page_data.user.profile.custom_url && page_data.user.profile.custom_url !== '')
    ? '/' + page_data.user.profile.custom_url
    : '/' + page_data.user._id;
%>

<!-- Stats + Quick links are injected globally in profile.ejs -->

<!-- Activity: latest movies -->
<div id="profile_activity" class="container" style="margin-top: 24px;">
  <h3>Latest watched activity</h3>
  <div id="movies_holder" class="row"></div>
</div>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    profileMoviesLatest('<%= page_data.user._id%>')
  });
</script>

<% include movie_template.ejs %>
